---
import '../styles/global.css';
import SEO from '../components/SEO.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import BackToTop from '../components/BackToTop.astro';
import CapyCompanion from '../components/CapyCompanion.astro';
import CapyScreenWalker from '../components/CapyScreenWalker.astro';

export interface Props {
  title: string;
  description: string;
  canonical?: string;
  image?: string;
  type?: 'website' | 'article';
  publishedTime?: string;
  modifiedTime?: string;
  author?: string;
  section?: string;
  tags?: string[];
  noindex?: boolean;
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
  /** Set false on the article page, which renders its own <main>/<article>. */
  wrapMain?: boolean;
}

const { wrapMain = true, ...seo } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#030E50" media="(prefers-color-scheme: dark)" />
    <meta name="theme-color" content="#F9FBFE" media="(prefers-color-scheme: light)" />
    <link rel="sitemap" type="application/xml" href="/sitemap.xml" />
    <SEO {...seo} />

    {/* No-flash theme: runs before first paint, so the correct palette is
        applied synchronously from localStorage / OS preference. */}
    <script is:inline>
      (function () {
        try {
          var stored = localStorage.getItem('theme');
          if (stored === 'light' || stored === 'dark') {
            document.documentElement.setAttribute('data-theme', stored);
          }
        } catch (e) {}
      })();
    </script>
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>
    <Header />
    {
      wrapMain ? (
        <main id="main">
          <slot />
        </main>
      ) : (
        <slot />
      )
    }
    <Footer />
    <BackToTop />
    <CapyCompanion />
    <CapyScreenWalker />
  </body>
</html>
